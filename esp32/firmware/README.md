# Firmware split

Este folder ya no contiene sketches compilables.

Usa uno de estos folders:

- `esp32/firmware_http/` para el firmware que envía audio por HTTP.
- `esp32/firmware_serial/` para el firmware que transmite por Serial.

Abre el archivo `.ino` del folder que quieras compilar en Arduino IDE.